# opencode-token-usage 路线图

为 **OpenCode 2** 提供轻量级、实时的 token 用量监视器。勾选项表示已实现且通过相应验证；未来版本的示例仅用于说明设计。

## v0.1.0 — 核心用量与基础侧边栏

目标：提供可本地安装的完整会话树用量面板，不包含 npm 发布。

* [x] OpenCode 2.0.9 主插件与 `./tui` 入口集成
* [x] 向原生 `sidebar.content` 追加基础面板，保留宿主内容
* [x] 子代理视图使用 `session.composer.top` 显示完整面板，适配宿主隐藏侧边栏的行为
* [x] 检测当前会话，沿 `parentID` 汇总根会话及所有后代
* [x] 分页读取完整消息，包括压缩前历史和未打开的子代理
* [x] 快照替换与继承历史去重，防止重复累计
* [x] 显示 Input、Output、Reasoning、Cache Read、Cache Write
* [x] 显示缓存命中率 Cache Rate
* [x] 显示五类 token 的总量 Total
* [x] 按当前查看会话的活动模型估算 Cost，支持模型切换
* [x] 未选模型时使用所在位置的默认模型；缺失适用价格按 0 并标注
* [x] 按每条消息的传入 token 数选择价格阶梯
* [x] 基础数字格式化：`1200 → 1.2K`、`125000 → 125K`、`1500000 → 1.5M`
* [x] 空会话保留全部统计行，显示零
* [x] 消息用量上报后合并刷新，切换会话时隔离旧请求
* [x] 加载、不可用、未更新与失败恢复状态
* [x] 实际打包产物在 OpenCode 2.0.9 中验证加载、显示、刷新与子代理累计

预览及安装说明见 [README.md](README.md)，验证结果见 [验证记录](docs/verification.md)。

## v0.2.0 — 上下文用量

目标：让上下文消耗情况清晰可见。

* [ ] 读取活动模型的上下文窗口大小
* [ ] 计算当前上下文用量
* [ ] 显示已用 / 最大上下文
* [ ] 显示上下文用量百分比和进度条
* [ ] 处理上下文限制未知的模型
* [ ] 在模型切换后同步更新上下文指标

```text
Context       72.4K / 128K
███████████░░░░░░░ 56.6%
```

## v0.3.0 — TUI 体验改进

基础侧边栏与语义主题颜色已纳入首版，本阶段完善布局与性能。

* [ ] 针对窄终端的响应式布局
* [ ] 可配置的不可用 token 类别隐藏
* [ ] 改进超大数值的格式化显示
* [ ] 可配置的紧凑模式
* [ ] 可配置的侧边栏可见性
* [ ] 完善主题适配
* [ ] 超大历史的增量读取与渲染优化

紧凑布局示例：

```text
Token    98.1K
Context  56.6%
Cost     $0.21
```

## v0.4.0 — 用量命令

目标：按需提供比侧边栏更详细的 token 信息。

* [ ] 添加 `/usage`
* [ ] 显示当前会话及会话树统计
* [ ] 显示 token 明细与上下文用量
* [ ] 显示模型和价格信息
* [ ] 显示估算费用明细

## 后续评估

* [ ] 公开价格目录与更新策略
* [ ] npm 发布流程
