# opencode-token-usage 路线图

为 **OpenCode 2** 提供轻量级、实时的 token 用量监视器。勾选项表示已实现且通过相应验证；未来版本的示例仅用于说明设计。

## v0.1.0 — 核心用量与基础侧边栏

目标：提供可本地安装的完整会话树用量面板，不包含 npm 发布。

* [x] OpenCode 2.0.9 主插件与 `./tui` 入口集成
* [x] 向原生 `sidebar.content` 追加基础面板，保留宿主内容
* [x] 子代理视图通过 `session.composer.top` 提供使用量入口，适配宿主隐藏侧边栏的行为（初版显示完整面板，后改为按需弹窗）
* [x] 检测当前会话，沿 `parentID` 汇总根会话及所有后代
* [x] 分页读取完整消息，包括压缩前历史和未打开的子代理
* [x] 快照替换与继承历史去重，防止重复累计
* [x] 显示 Input、Output、Reasoning、Cache Read、Cache Write
* [x] 显示缓存命中率 Cache Rate
* [x] 显示五类 token 的总量 Total
* [x] 按当前查看会话的活动模型估算 Est. Cost，支持模型切换
* [x] 未选模型时使用所在位置的默认模型；缺失适用价格按 0 并标注
* [x] 按每条消息的传入 token 数选择价格阶梯
* [x] 数字使用千位逗号分组：`1200 → 1,200`、`125000 → 125,000`、`1500000 → 1,500,000`
* [x] 空会话保留基础统计行并显示零；零值 Cache Write 与 Est. Cost 隐藏
* [x] 消息用量上报后合并刷新，切换会话时隔离旧请求
* [x] 加载、不可用、未更新与失败恢复状态
* [x] 实际打包产物在 OpenCode 2.0.9 和 2.0.10 中验证加载、显示、刷新与子代理累计

预览及安装说明见 [README.md](README.md)，验证结果见 [验证记录](docs/verification.md)。

## v0.2.0 — 上下文用量

目标：让上下文消耗情况清晰可见。

* [x] 读取活动模型的上下文窗口大小
* [x] 显示已用 / 最大上下文
* [x] 显示上下文用量百分比
* [x] 处理上下文限制未知的模型
* [x] 在模型切换后同步更新上下文指标

```text
Context  72,400 / 128,000 (56.6%)
```

上下文行显示在面板第一行；百分比与用量同行，进度条留待后续版本。

## v0.2.1 — TPS、TTFT的显示

目标：提供更直观的性能指标。

* [x] 显示实时 TPS（每秒 token 处理量）
* [x] 显示平均 TTFT（首 token 处理时间）

```text
TPS  48.7 tok/s
TTFT  2.7s
```

## v0.2.5 — Steps 计数

目标：让整棵树的模型调用次数可见。

* [x] 显示全树（含子代理）的 `Steps` 计数
* [x] 口径与 OpenCode 官方统计一致：每条 assistant 消息为一个 step，compaction 与用户消息不计
* [x] 分叉继承副本不重复计数
* [x] 行位置为 Context 之后、Input 之前；无 Context 时位于面板第一行
* [x] 实际打包产物在 OpenCode 2.0.11 中验证显示与刷新

```text
Steps  1,234
```

## v0.3.0 — 官方价格补全

目标：按每条消息实际使用的模型统计费用，并在 OpenCode 缺少价格时使用厂商官方价格补全。

* [x] 内置可追溯的厂商官方价格快照
* [x] 首版收录最近半年发布且具有明确标准 token 定价的模型
* [x] OpenCode 完整适用价格优先，缺失时整条消息回退到官方价格
* [x] 按 assistant 与 compaction 消息实际模型分别计算
* [x] 支持价格阶梯、缓存读写和 Reasoning 费用
* [x] 支持网关模型到厂商模型的精确 ID 与明确别名映射
* [x] 区分免费、部分可计算和价格不可用
* [x] 已收录模型永久保留，后续持续核验价格和来源
* [x] 通过单元测试与 OpenCode 2.0.11 打包集成验证

## v0.3.2 — GPT-6 Sol / Luna 定价

* [x] 根据 OpenAI 官方 Standard 价格补全两款模型的短、长上下文费率
* [x] 将 OpenAI 长上下文切换边界校正为传入 token 超过 272,000
* [x] 通过类型检查、45 项自动化测试及构建

## v0.4.0 — 用量命令

目标：按需提供比侧边栏更详细的 token 信息。以弹窗的形式显示。

* [x] 添加 `/usage`
* [x] 显示当前会话树的 token 信息
* [x] 显示 context 各项占比
* [x] 显示 token 明细与上下文用量
* [x] 显示各个模型费用
* [x] 拆分为 Context Window / Last Request / Context Breakdown / Session / By Model，口径互不混用
* [x] 紧凑数字（K/M）、来源条形图、Session 单行汇总、`d` 详细模式切换
* [x] 子代理视图保留 Context、Total、Cost、TPS 单行摘要，点击打开可关闭的原生居中统计弹窗

## 后续评估

* [x] 首次 npm 发布：`@chenlongapps/opencode-token-usage@0.2.1`

## 待解决问题

* [ ] TPS 的实时计算：当前为近似值，后续考虑通过更精确的算法计算